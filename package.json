{
  "name": "dmicher-master-screen-development",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --import ./scripts/esm-loader.mjs --test tests/*.test.js",
    "deploy:dry-run": "node scripts/release.mjs dry-run",
    "deploy": "node scripts/release.mjs deploy",
    "release:build": "node scripts/release.mjs build",
    "release:verify": "node scripts/release.mjs verify"
  }
}
